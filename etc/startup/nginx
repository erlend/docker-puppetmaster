#!/bin/sh

ssldir=/var/lib/puppet/ssl
certname=${NGINX_CERTNAME:-`hostname -f`}

# Set correct certificte
sed -e "s|\(ssl_certificate *$ssldir/certs/\).*|\1$certname.pem;|" \
    -e "s|\(ssl_certificate_key *$ssldir/private_keys/\).*|\1$certname.pem;|" \
    -i /etc/nginx.conf

